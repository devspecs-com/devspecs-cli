# DevSpecs Task Raw Output Samples

These samples were generated from the current local `ds.exe` for sharing and workflow review.

## generated-a-series

`generated-a-series/ds-task-workflow-ux-boilerplate` is the raw CLI output from:

```text
ds task "Capture preferred ds task workflow UX for series, slices, iterations, lifecycle decisions, and learnings" --id ds-task-workflow-ux-boilerplate --dir .devspecs/raw-output-samples/generated-a-series --slice "define series and slice lifecycle model" --slice "scaffold iteration-plan artifacts" --slice "record completion supersedence split and rollback decisions" --slice "surface learnings as bounded checkpoint evidence" --index=false --force
```

It is intentionally not an active task workspace.

## renamed-b-series

`renamed-b-series/ds-task-workflow-ux-boilerplate` is a mechanical copy of the generated A-series sample with `A00/A01/...` references renamed to `B00/B01/...`.

This is not directly generated by the CLI today. It exists to show what a later B-series might look like if the workflow supports multiple named series inside a broader task thread.
