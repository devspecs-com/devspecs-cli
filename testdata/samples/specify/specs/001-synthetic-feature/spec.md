# Synthetic Feature Spec

## User Story

As a developer, I want a fixture that resembles a Spec Kit feature folder so
format and layout-group detection can be tested.

## Requirements

- The fixture must be small.
- The fixture must not describe unreleased roadmap behavior.
